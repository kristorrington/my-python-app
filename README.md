# My Python App

A simple Python project to demonstrate AI Agent capabilities using the Model Context Protocol (MCP).

## Setup
1. Install dependencies: `pip install requests`
2. Run the application: `python main.py`

